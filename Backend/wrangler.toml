name = "focusphone-api"
main = "src/index.js"
compatibility_date = "2024-01-01"

# Secrets (set via `wrangler secret put`):
#   GROQ_API_KEY   — real Groq API key
#   APP_SECRET     — shared secret the iOS app sends as Bearer token
